class Park {
  constructor(name, coords, distance, images/*, food, plants*/) {
    this.name = name;
    this.coords = coords;
    this.distance = distance;
    this.images = images;
  }
}

export default Park;